{
  "name": "tail-screen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An iPad-friendly Tailscale media remote and launcher.",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
